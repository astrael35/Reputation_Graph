{
  "name": "reputation_graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ reputation_graph",
    "create-local": "graph create --node http://localhost:8020/ reputation_graph",
    "remove-local": "graph remove --node http://localhost:8020/ reputation_graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 reputation_graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.81.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.35.1",
    "matchstick-as": "0.5.0"
  }
}
